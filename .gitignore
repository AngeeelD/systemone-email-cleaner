# Secrets — never commit
client_secret.json
token.json

# Local config (endpoint and thresholds are machine-specific)
config.yaml

# Audit logs — contain message subjects and sender addresses
audit/

# Build output (anchored: unanchored "emailcleaner" also matched cmd/emailcleaner/)
/emailcleaner

# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/
